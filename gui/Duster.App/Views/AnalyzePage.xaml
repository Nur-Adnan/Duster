<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Duster.App.Views.AnalyzePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel MaxWidth="960" Padding="{StaticResource PagePadding}" HorizontalAlignment="Stretch" Spacing="4">
        <TextBlock Style="{StaticResource PageTitleStyle}" Text="Analyze" />
        <TextBlock Style="{StaticResource SecondaryTextStyle}" Text="See which folders and files use the most space, and what changed since the last scan." />
        <Border Margin="0,16,0,0" Style="{StaticResource SectionPanelStyle}">
            <StackPanel Spacing="8">
                <TextBlock Style="{StaticResource BodyTextBlockStyle}" Text="Analyzing from this window is not available yet. Run this in a terminal instead:" TextWrapping="Wrap" />
                <TextBlock
                    FontFamily="Consolas"
                    IsTextSelectionEnabled="True"
                    Text="{x:Bind ViewModel.CliCommand}"
                    AutomationProperties.AutomationId="AnalyzeCliCommand" />
            </StackPanel>
        </Border>
    </StackPanel>
</Page>
